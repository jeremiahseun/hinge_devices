/// Hinge-angle driven effects.
///
/// Deliberately a separate import from `foldable_runtime.dart`. Angle is a
/// continuous signal meant for interactions and visual effects — parallax,
/// reveals, progressive disclosure. Layout belongs to posture.
library;

export 'src/widgets/hinge_angle_builder.dart';
